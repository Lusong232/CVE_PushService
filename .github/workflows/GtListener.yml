name: Github CVE Listener

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install requests serverchan-sdk

    - name: Run CVE Monitor
      env:
        SCKEY: ${{ secrets.SCKEY }}
        GH_TOKEN: ${{ secrets.GH_TOKEN }}
      run: |
        ls -la
        python CVE_GhListener.py

    - name: Commit database changes
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        # 添加数据库文件和 JSON 文件（如果存在）
        git add Github_CVE_Monitor.db docs/latest_gh.json || true
        git diff --staged --quiet || (git commit -m "Update CVE database and latest messages" && git push)
